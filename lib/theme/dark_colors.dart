import 'package:flutter/material.dart';

// 다크 테마 색상 추천
const Color darkPrimaryColor = Color(0xFF88B04B);         // 다크 테마 메인 녹색 (라이트 대비 약간 어둡고 부드러운 톤)
const Color darkSecondaryColor = Color(0xFF627D32);       // 보조 녹색 (딥 올리브 그린 톤)
const Color darkBackgroundColor = Color(0xFF121212);      // 다크 배경 (전형적인 다크모드 배경)
const Color darkCardColor = Color(0xFF1E1E1E);            // 카드 배경 (약간 더 밝은 회색)
const Color darkTextColor = Color(0xFFECECEC);            // 기본 텍스트 (거의 흰색)
const Color darkSubTextColor = Color(0xFF9E9E9E);          // 보조 텍스트 (중간 회색)
const Color darkAppBarColor = Color(0xFF1B1F1B);           // 앱바 배경 (녹색 톤 살짝 입힌 짙은 그레이)
const Color darkAppBarTextColor = Color(0xFFECECEC);       // 앱바 텍스트 (기본 텍스트와 같음)
const Color darkErrorColor = Color(0xFFEF5350);            // 에러 색상 (라이트와 비슷하게 유지)
const Color darkSwitchActiveColor = Color(0xFF88B04B);     // 스위치 활성화 (메인과 동일)
const Color darkSwitchInactiveColor = Color(0xFF616161);   // 스위치 비활성화 (짙은 회색)
